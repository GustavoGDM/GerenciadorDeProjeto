I"A(function() {
  $(window).resize(function() {
    var altura, height, largura, largura_tela, tela;
    tela = $(window);
    largura_tela = tela.width();
    altura = (1000 * largura_tela) / 2000;
    largura = $(".landing")[0];
    console.log(largura);
    return largura.style(height = "100px");
  });

}).call(this);
:ET