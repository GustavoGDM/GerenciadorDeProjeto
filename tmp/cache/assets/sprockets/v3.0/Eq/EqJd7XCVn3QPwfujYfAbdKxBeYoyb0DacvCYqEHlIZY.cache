I"-(function() {
  $(window).resize(function() {
    var altura, height, largura, largura_tela, tela;
    tela = $(window);
    largura_tela = tela.width();
    altura = (1000 * largura_tela) / 2000;
    largura = $(".landing");
    return largura[0].style(height = altura + "px");
  });

}).call(this);
:ET