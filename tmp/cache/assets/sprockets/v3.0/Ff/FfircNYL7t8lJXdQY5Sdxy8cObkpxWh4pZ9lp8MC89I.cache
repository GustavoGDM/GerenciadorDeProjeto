I"K(function() {
  $(window).resize(function() {
    var altura, largura, largura_tela, tela;
    tela = $(window);
    largura_tela = tela.width();
    altura = (1000 * largura_tela) / 2000;
    largura = $(".landing")[0];
    largura.style.height = altura + "px";
    return console.log(largura.style.height);
  });

}).call(this);
:ET